{
  "name": "@tether/server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "bin": {
    "tether": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json && cp src/machine/tether.conf dist/machine/tether.conf && chmod +x dist/cli.js",
    "prepare": "npm run build -w @tether/shared && npm run build"
  },
  "dependencies": {
    "@fastify/cookie": "^11.1.2",
    "@tether/shared": "*",
    "fastify": "^5.10.0"
  }
}
