# Copy to .env and fill in. Never commit .env.
# Nothing here is read yet — the server arrives in PR #4.

# Address the tether server binds to. Loopback by default; binding anywhere
# else is an explicit choice and requires a password to be set.
TETHER_HOST=
TETHER_PORT=
